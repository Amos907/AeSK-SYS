import React from "react";

export const Footer = (header, subheader) => {
  return (
    <footer>
      <small>© {new Date().getFullYear()} Aeronotical Society of Kenya</small>
    </footer>
  );
};
